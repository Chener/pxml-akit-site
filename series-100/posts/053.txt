停止向AI许愿 · 53/100

用户连点两下，订单两份——重复请求没被挡。
口令：Dedup（去重）

场景：同一业务请求只应生效一次。
例如：changelog 标明破坏性变更。对比 Spec：先对齐预期。默认静默改行为——口令纠正。

直接复制给 AI：
「为______做 Dedup：
- 去重键怎么算（哪些字段）
- 窗口多久
- 重复到达时返回首次结果还是明确「重复」
禁止只靠前端防抖；必须服务端生效。
输出：键设计 + 存储 + 我怎么连发两次验 3 步。」

同样的请求，只认一次。
上一条 52/100 · 下一条 54/100 → Rollback
